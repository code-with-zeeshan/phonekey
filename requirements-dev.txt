# requirements-dev.txt — Development + build dependencies
# Install with: pip install -r requirements-dev.txt

# Runtime
-r requirements.txt

# Build (creates standalone .exe / binary)
pyinstaller>=6.10.0

# Code quality
ruff==0.3.0