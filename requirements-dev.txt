# requirements-dev.txt — Development + build dependencies
# Install with: pip install -r requirements-dev.txt

# Runtime
-r requirements.txt

# Build (creates standalone .exe / binary)
pyinstaller>=6.10.0

# Code quality
ruff==0.3.0

# Note: Cloudflare Quick Tunnels requires cloudflared binary
# Download from: https://github.com/cloudflare/cloudflared/releases
# Not a Python package - standalone binary only